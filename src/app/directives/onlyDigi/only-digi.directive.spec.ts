import { OnlyDigiDirective } from './only-digi.directive';

describe('OnlyDigiDirective', () => {
  it('should create an instance', () => {
    // const directive = new OnlyDigiDirective();
    expect(directive).toBeTruthy();
  });
});
